---
title: "bb - Manage git tools (branches, commits, etc.)"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{bb-manage-git-tools--branches-commits-etc--}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(lozen)
```

<!-- WARNING - This vignette is generated by {fusen} from dev/flat_manage_git.Rmd: do not edit by hand -->

# Define local installation and clone

This code will clone the project locally, in a temporary directory.


  

```{r example-clone_locally, eval = FALSE}
#' \dontrun{
project_path <- clone_locally(
  project_name = the_project[["name"]],
  group_url = group_url,
  open = TRUE
)
#' }
```

  

# Add commit templates

  

```{r example-add_git_templates, eval = FALSE}
#' \dontrun{
add_git_templates(
  project_path = project_path,
  type = c("commit", "mr")
)
#' }
```

  

  

# Manage branches

## Push to server

  

```{r example-push_all_to_branch, eval = FALSE}
#' \dontrun{
push_all_to_branch(
  project_path = project_path
)
#' }
```

  

## Start _main_ and create a _production_ branch

  

  

```{r example-create_production, eval = FALSE}
#' \dontrun{
create_production(
  project_path = project_path
)
#' }
```

  

  


